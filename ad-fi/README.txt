The ad-fi is an older implementation of solvers based on automatic differentiation that is deprecated. We recommend that users instead use the ad-core and ad-blackoil modules, which contain the same functionality in a more modular framework.

