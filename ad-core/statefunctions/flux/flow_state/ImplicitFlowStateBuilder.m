classdef ImplicitFlowStateBuilder < FlowStateBuilder
    % Default base class is already implicit - we have this class to make
    % it explicit in case we would change the default
    properties
        
    end
    
    methods

    end
end
