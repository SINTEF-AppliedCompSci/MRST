function I = evaluateMonomialIntegralV2(normals, X, Xmid, m)

    I = zeros(1,6);
    I(1) = 1;
    Ne = size(normals);
    for e = 1:Ne
        nVec = normals(e);
        I = I + 1/6*m(X() + 2/3m(Xmid)

end