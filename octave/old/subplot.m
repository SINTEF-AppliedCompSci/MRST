function subplot(rows,cols,index,varargin)
  %modifed subplot for octave
     if(isstr(rows))
       figure();
     else
       %subplot_org(rows,cols,index)
       %builtin('subplot',rows,cols,index);
     end
end